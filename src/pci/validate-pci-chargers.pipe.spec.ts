import { ValidatePciChargersPipe } from './validate-pci-chargers.pipe';

describe('ValidatePciChargersPipe', () => {
  it('should be defined', () => {
    expect(new ValidatePciChargersPipe()).toBeDefined();
  });
});
